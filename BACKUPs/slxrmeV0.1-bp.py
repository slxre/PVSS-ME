import time


# QD
user = ('member', )
MAC = 'gnrlaccess', 'Xi004', 'SLxREx'
UEC = 'egenrlacces', '400iX', 'SLxRExx'
exitt = 'E'
mact1 = True
AAC = 'SLxRExx'
AEC = 'xxSLxRE'
inputt2 = 'r'

######################################################################################################

print("WELCOME 2 SLXRme -V0.0.1")
time.sleep(2.5)
print("Loading Setup...")

from time import sleep
def loadbar(iteration, total, prefix='', suffix='', decimals=1, length=100, fill='█'):
    percent = ("{0:." + str(decimals) + "f}").format(100 * (iteration / float(total)))
    filledLength = int(length * iteration // total)
    bar = fill * filledLength + '-' * (length - filledLength)
    print('\r%s |%s| %s%% %s' % (prefix, bar, percent, suffix), end='\r')
    # Print New Line on Complete
    if iteration == total:
        print()

items = list(range(0, 50))
l = len(items)

loadbar(0, l, prefix='Progress:', suffix='Complete', length=l)
for i, item in enumerate(items):
   sleep(0.1)
   loadbar(i + 1, l, prefix='Progress:', suffix='Complete', length=l)

######################################################################################################

whois = input('Who Are You?: ').lower()

if user == ('member'.lower(), 'SLxRE'.lower(), 'Agent'.lower()):
    print('loading', whois)
    time.sleep(2.5)
    from time import sleep    


def loadbar(iteration, total, prefix='', suffix='', decimals=1, length=100, fill='█'):
    percent = ("{0:." + str(decimals) + "f}").format(100 * (iteration / float(total)))
    filledLength = int(length * iteration // total)
    bar = fill * filledLength + '-' * (length - filledLength)
    print('\r%s |%s| %s%% %s' % (prefix, bar, percent, suffix), end='\r')
    # Print New Line on Complete
    if iteration == total:
        print()

items = list(range(0, 50))
l = len(items)

loadbar(0, l, prefix='Progress:', suffix='Complete', length=l)
for i, item in enumerate(items):
   sleep(0.1)
   loadbar(i + 1, l, prefix='Progress:', suffix='Complete', length=l)

######################################################################################################

user = input('Enter Password?: ').lower()
if user == 'gnrlaccess'.lower():
    print('Welcome ', whois, 'to ', 'SLxRme -V0.0.1')
    time.sleep(3)
    print('Launching...')
    from time import sleep
    
def loadbar(iteration, total, prefix='', suffix='', decimals=1, length=100, fill='█'):
        percent = ("{0:." + str(decimals) + "f}").format(100 * (iteration / float(total)))
        filledLength = int(length * iteration // total)
        bar = fill * filledLength + '-' * (length - filledLength)
        print('\r%s |%s| %s%% %s' % (prefix, bar, percent, suffix), end='\r')
        # Print New Line on Complete
        if iteration == total:
            print()

items = list(range(0, 50))
l = len(items)

loadbar(0, l, prefix='Progress:', suffix='Complete', length=l)
for i, item in enumerate(items):
                sleep(0.1)
                loadbar(i + 1, l, prefix='Progress:', suffix='Complete', length=l)
                print("You Are Not Recognized By The Board and Our Leaders")
                time.sleep(4)
                print("SLxRiVN Registry Costs $100,000,000")
                time.sleep(2)
                print("The Bank is Curretly Closed Until The Next Election. See You Then.")
                time.sleep(8)
                user = input("Type E to Exit: ").lower()
                if user == 'E.'.lower():
                                print(" Launching Process...")
                                user = input('Enter Password: ').lower()
                                if user == 'gnrlaccess'.lower():
                                        print('Exiting...')
                                        time.sleep(2)
                                        from time import sleep    
                                        
def loadbar(iteration, total, prefix='', suffix='', decimals=1, length=100, fill='█'):
                                            percent = ("{0:." + str(decimals) + "f}").format(100 * (iteration / float(total)))
                                            filledLength = int(length * iteration // total)
                                            bar = fill * filledLength + '-' * (length - filledLength)
                                            print('\r%s |%s| %s%% %s' % (prefix, bar, percent, suffix), end='\r')
                                            # Print New Line on Complete
                                            if iteration == total:
                                                print()

items = list(range(0, 50))
l = len(items)

loadbar(0, l, prefix='Progress:', suffix='Complete', length=l)
for i, item in enumerate(items):
                                                        sleep(0.1)
                                                        loadbar(i + 1, l, prefix='Progress:', suffix='Complete', length=l)

                                                        print("Goodbye ", whois, " See You Next Year!!")
                                                        time.sleep(2)
                                                        quit()
if user != 'gnrlaccess'.lower():
                                                            print("Invalid Password")
                                                            time.sleep(2)
                                                            print("Exiting...")
                                                            time.sleep(2)
                                                            from time import sleep
                                                            
def loadbar(iteration, total, prefix='', suffix='', decimals=1, length=100, fill='█'):
                                                                percent = ("{0:." + str(decimals) + "f}").format(100 * (iteration / float(total)))
                                                                filledLength = int(length * iteration // total)
                                                                bar = fill * filledLength + '-' * (length - filledLength)
                                                                print('\r%s |%s| %s%% %s' % (prefix, bar, percent, suffix), end='\r')
                                                                # Print New Line on Complete
                                                                if iteration == total:
                                                                    print()

items = list(range(0, 50))
l = len(items)

loadbar(0, l, prefix='Progress:', suffix='Complete', length=l)
for i, item in enumerate(items):
                                                                        sleep(0.1)
                                                                        loadbar(i + 1, l, prefix='Progress:', suffix='Complete', length=l)
                                                                        quit()

                                       

            ######################################################################################################

if user == 'SLxRE':
    print('Welcome Ruler ', whois, ' Your Platform: SLxRme is at -V0.0.1')
    time.sleep(3)
    print('Launching...')
    from time import sleep    
    def loadbar(iteration, total, prefix='', suffix='', decimals=1, length=100, fill='█'):
        percent = ("{0:." + str(decimals) + "f}").format(100 * (iteration / float(total)))
        filledLength = int(length * iteration // total)
        bar = fill * filledLength + '-' * (length - filledLength)
        print('\r%s |%s| %s%% %s' % (prefix, bar, percent, suffix), end='\r')
        # Print New Line on Complete
        if iteration == total:
            print()

            items = list(range(0, 50))
            l = len(items)

            loadbar(0, l, prefix='Progress:', suffix='Complete', length=l)
            for i, item in enumerate(items):
                sleep(0.1)
                loadbar(i + 1, l, prefix='Progress:', suffix='Complete', length=l)
                print("Welcome, SLxRE")
                time.sleep(3)
                print("There Are No New SLxRIVNs That Have Recently Joined. Check Back Later. ")
                time.sleep(4)
                print("New Member Has Joined. ", whois, + " You Are 10% Closer To Becoming A World Leader.")
                time.sleep(0.8)
                print("New Member Has Joined. ", whois, + " You Are 10% Closer To Becoming A World Leader.")
                time.sleep(0.8)
                print("New Member Has Joined. ", whois, + " You Are 10% Closer To Becoming A World Leader.")
                time.sleep(0.8)
                print("New Member Has Joined. ", whois, + " You Are 10% Closer To Becoming A World Leader.")
                time.sleep(0.8)
                print("New Member Has Joined. ", whois, + " You Are 10% Closer To Becoming A World Leader.")
                time.sleep(0.8)
                print("New Member Has Joined. ", whois, + " You Are 10% Closer To Becoming A World Leader.")
                time.sleep(0.8)
                user = input("Type E to Exit: ").lower()
                if user == 'E.'.lower():
                                print(" Launching Process...")
                                user = input('Enter Password: ').lower()
                                if user == 'gnrlaccess'.lower():
                                        print('Exiting...')
                                        time.sleep(2)

                                        from time import sleep    
                                        def loadbar(iteration, total, prefix='', suffix='', decimals=1, length=100, fill='█'):
                                            percent = ("{0:." + str(decimals) + "f}").format(100 * (iteration / float(total)))
                                            filledLength = int(length * iteration // total)
                                            bar = fill * filledLength + '-' * (length - filledLength)
                                            print('\r%s |%s| %s%% %s' % (prefix, bar, percent, suffix), end='\r')
                                            # Print New Line on Complete
                                            if iteration == total:
                                                print()

                                                items = list(range(0, 50))
                                                l = len(items)

                                                loadbar(0, l, prefix='Progress:', suffix='Complete', length=l)
                                                for i, item in enumerate(items):
                                                        sleep(0.1)
                                                        loadbar(i + 1, l, prefix='Progress:', suffix='Complete', length=l)

                                                        print("Goodbye ", whois, " See You Next Year!!")
                                                        time.sleep(2)
                                                        quit()
    if user != 'SLxRE':
                                                            print("Invalid Password")
                                                            time.sleep(2)
                                                            print("Exiting...")
                                                            time.sleep(2)
                                                            from time import sleep
                                                            def loadbar(iteration, total, prefix='', suffix='', decimals=1, length=100, fill='█'):
                                                                percent = ("{0:." + str(decimals) + "f}").format(100 * (iteration / float(total)))
                                                                filledLength = int(length * iteration // total)
                                                                bar = fill * filledLength + '-' * (length - filledLength)
                                                                print('\r%s |%s| %s%% %s' % (prefix, bar, percent, suffix), end='\r')
                                                                # Print New Line on Complete
                                                                if iteration == total:
                                                                    print()

                                                                    items = list(range(0, 50))
                                                                    l = len(items)

                                                                    loadbar(0, l, prefix='Progress:', suffix='Complete', length=l)
                                                                    for i, item in enumerate(items):
                                                                        sleep(0.1)
                                                                        loadbar(i + 1, l, prefix='Progress:', suffix='Complete', length=l)
                                                                        quit()
                            
                            
    if user != 'E'.lower():
                                                                            print("Starting Shutdown...")        
        

    else:
                                                                                print("DENiED!!")
                                                                                time.sleep(1.5)
                                                                                print("Try Again Some Other Time")
                                                                                quit()




